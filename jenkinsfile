pipeline {
    agent any
    stages { 
        stage('installation ') {
            steps {
                sh 'npm install'
            }
        }
        stage('run cypress test ') {
            steps {
                sh 'npm run cypress:ads'
            }
        }
    }
}
